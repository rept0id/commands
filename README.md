# commands

| Command | Enviroments | Description |
|--- |--- |--- |
| traceroute example.com | Linux | Path data take across a network to reach example.com | 
| netstat | Linux | Network statistics (e.g. connections, listening ports, routing table) |
| arp -a | Linux | Devices recently communicated with on the local network |
| nmap -sn 192.168.1.0/24 | Linux | Devices on the local network on 192.168.1.0/24 subnet |
| nslookup example.com | Linux | DNS data |
| strace -c ls | Linux | System calls data for ls |
